<Window x:Class="Projekt_Poig.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Projekt_Poig"
        mc:Ignorable="d"
        xmlns:vm="clr-namespace:Projekt_Poig.ViewModel"
        xmlns:view="clr-namespace:Projekt_Poig.View"
        Title="MainWindow" Height="450" Width="800">


    <Grid>

        <Grid.Resources>
            <vm:MainViewModel x:Key="mainViewModel"/>
        </Grid.Resources>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <Grid Grid.Row="0">
            <Button Grid.Column="0" Width="50" Content="Gry" Command="{Binding NavigateGryCommand}" HorizontalAlignment="Left"/>
            <Button Grid.Column="1" Width="50" Content="Atrybuty" Command="{Binding NavigateAtrybutyCommand}" HorizontalAlignment="Left" Margin="50,0,0,0"/>
            <Button Grid.Column="2" Width="50" Content="Typy" Command="{Binding NavigateTypyCommand}" HorizontalAlignment="Left" Margin="100,0,0,0"/>
        </Grid>

        <Grid Grid.Row="1" >
            <ContentControl Content="{Binding CurrentViewModel}">
                <ContentControl.Resources>
                    <DataTemplate DataType="{x:Type vm:AtrybutViewModel}">
                        <view:Atrybuty_tabela/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vm:GryViewModel}">
                        <view:Gry_tabela/>
                    </DataTemplate>
                    <DataTemplate DataType="{x:Type vm:TypyViewModel}">
                        <view:Typy_tabela/>
                    </DataTemplate>
                </ContentControl.Resources>
            </ContentControl>
        </Grid>
    </Grid> 
</Window>