<Window x:Class="Projekt_Poig.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Projekt_Poig"
        mc:Ignorable="d"
        xmlns:vm="clr-namespace:Projekt_Poig.ViewModel"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <vm:MainViewModel x:Key="mainViewModel"/>
    </Window.Resources>
    <Grid DataContext="{StaticResource mainViewModel}">
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <Grid Grid.Row="0">
            <Button Grid.Column="0" Width="50" Content="Gry" HorizontalAlignment="Left"/>
            <Button Grid.Column="1" Width="50" Content="Atrybuty" HorizontalAlignment="Left" Margin="50,0,0,0"/>
            <Button Grid.Column="2" Width="50" Content="Typy" HorizontalAlignment="Left" Margin="100,0,0,0"/>
        </Grid>
        <Grid x:Name="panel" Grid.Row="1">
        </Grid>
    </Grid> 
</Window>